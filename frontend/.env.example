# URL del backend de AthenIA.
#
# Desarrollo local: dejar comentado. El frontend usa "/api" y el proxy de Vite
# (definido en vite.config.js) redirige a http://127.0.0.1:8000.
#
# Despliegue en OCI: descomentar y apuntar a la IP/dominio del backend.
# VITE_API_URL=http://<IP-PUBLICA-OCI>:8000
