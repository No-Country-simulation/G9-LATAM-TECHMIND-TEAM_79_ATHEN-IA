"""
Implementaciones de `domain.protocols.RepositorioContenidos`.

Hoy solo existe `memoria.RepositorioMemoria`. En la Semana 3+ se agrega
`oracle.RepositorioOracle` (Autonomous Database) cumpliendo el mismo
`Protocol`, sin que rutas ni servicios cambien una linea.
"""
